class TemporalModelLoader:
    """
    Placeholder abstraction for future learned temporal classifiers.

    In a future iteration, this can load models (e.g., logistic regression,
    decision tree, threshold learner) from disk/model registry.
    """

    def load(self) -> None:
        return None
