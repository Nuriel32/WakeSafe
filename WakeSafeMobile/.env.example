# WakeSafe mobile runtime env (Expo public vars)
EXPO_PUBLIC_ENV=development

# Optional endpoint overrides (if unset, app uses values from environment.ts)
EXPO_PUBLIC_API_BASE_URL=
EXPO_PUBLIC_WS_URL=
